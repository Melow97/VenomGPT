# Supabase setup

1. Create a Supabase project.
2. Open SQL Editor and run schema.sql.
3. In Authentication > Providers enable Google.
4. In Authentication > URL Configuration add your local and production redirect URLs.
5. Keep the Supabase publishable/anon key in the browser only; never expose the service role key.
